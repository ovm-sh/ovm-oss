# Shipped — August 2026

| Feature | Shipped | Summary |
|---------|---------|---------|
| Truthful `clean --all` wording | 2026-08-13 | `ovm clean` now says it cleaned cached download artifacts (and that installed versions are untouched) instead of claiming version removal; the zero-byte case says "No cached download artifacts to clean". |
| A way to fully leave a product | 2026-08-13 | `ovm uninstall <product> --all` removes every installed version including the active one, clears the selection (current symlink + pin), and sweeps unlisted leftovers; typed product-name confirmation, `--yes` for scripts, non-interactive shells get a refusal with instructions. |
| `install` mentions an existing foreign install | 2026-08-13 | A standard `ovm install` now prints a one-line pointer when an unmanaged copy of the product exists on PATH (it may shadow the managed install; see `ovm adopt`). |
